# CRM Demo — نقشه راه شخصی‌سازی شده

> **تاریخ:** ۲۱ شهریور ۱۴۰۵
> **فلسفه:** بهترین ایده‌های sotcrm رو ساده‌تر و هوشمندتر اجرا می‌کنیم.

---

## وضعیت فعلی (v0.7)

| بخش | وضعیت |
|---|---|
| ✅ داشبورد | کارت هشدار + AI + Pipeline + آمار |
| ✅ تخته معاملات | Kanban + List + Smart Gates |
| ✅ مشتریان | جدول + فیلتر + فرم |
| ✅ جزئیات معامله | تایم‌لاین + Smart Gates + فرم فعالیت |
| ✅ Design System | ۱۳ کامپوننت قابل استفاده مجدد |
| ✅ Responsive | تمام صفحات سازگار |
| ✅ Loading States | Skeleton + PageLoader |

---

## Phase 1 — صفحه «امروز» + ⌘K (اولویت فوری)

> **درس از sotcrm:** مهم‌ترین صفحه، صفحه «امروز» هست — کاربر هر روز صبح اول اینو ببینه.

### 1.1 صفحه «امروز» (TodayView)
سطل‌های ساده‌شده برای ما:

| سطل | محتوا | منبع |
|---|---|---|
| 🔴 **عقب‌افتاده** | فعالیت‌های سررسید گذشته | `activities.is_done === false && due < today` |
| ⚠️ **در خطر** | معاملاتی که سلامتشون پایینه | `deals.health < 40` |
| 📞 **بدون تماس** | سرنخ‌های جدید بدون تماس | `deals.stage === 'lead' && noHumanTouch` |
| ✅ **امروز** | فعالیت‌های امروز | `activities.due === today` |
| 🎯 **احتمال بالا** | معاملات آماده بستن | `deals.probability >= 70` |

**طراحی:**
- هدر: «امروز — ۲۱ شهریور» + تعداد کل اقدامات
- هر سطل: عنوان + تعداد + لیست کارت‌های کوچک
- هر کارت: نام مشتری + عنوان معامله + دکمه اقدام سریع
- کلیک روی کارت → باز شدن DealDetail

### 1.2 جستجوی سراسری (⌘K)
- `Ctrl+K` یا `Cmd+K` → دیالوگ جستجو
- جستجو در: مخاطبین، معاملات، فعالیت‌ها
- نتایج گروه‌بندی شده با آیکون
- Enter → باز کردن نتیجه
- Escape → بستن

### 1.3 ثبت سریع (Quick Add)
- دکمه `+` در Sidebar → فرم ساده ۳ فیلدی:
  - شماره تماس (اختیاری)
  - نام مشتری
  - عنوان معامله
- Enter → ذخیره + رفتن به صفحه معامله

---

## Phase 2 — Post-Call Capture + ثبت فعالیت

> **درس از sotcrm:** بعد از هر تماس، ثبت نتیجه باید ۳ ثانیه بیشتر طول نکشه.

### 2.1 جریان ثبت سریع (S1-S5 ساده‌شده)
بعد از کلیک «ثبت تماس» روی یک معامله:

| مرحله | محتوا |
|---|---|
| S1 | نتیجه: پاسخ داد ✓ / پاسخ نداد ✗ / مشغول بود ⏳ / شماره اشتباه ❌ |
| S2 | وضعیت: علاقه‌مند / بعداً / منصرف / ارجاع به فروش |
| S3 | یادداشت (اختیاری — فیلد متنی) |
| S4 | اقدام بعدی: فردا ۱۰:۰۰ / ۳ روز دیگه / هفته بعد / سفارشی |
| S5 | ذخیره → بروزرسانی تایم‌لاین + نمایش در «امروز» |

**طراحی:**
- دیالوگ modal با ۵ صفحه
- هر صفحه ۲-۴ دکمه بزرگ
- فقط Tap/Click — تایپ حداقلی
- آخرین مرحله: «ذخیره و بستن» یا «ذخیره و بعدی»

### 2.2 کنترل‌های روی کارت معامله
 hovering روی کارت در Pipeline:
- 📞 تماس (باز کردن S1-S5)
- 💬 پیامک (باز کردن قالب)
- 📝 یادداشت (فیلد سریع)
- ➡️ مرحله بعد (Smart Gate)

---

## Phase 3 — گردش‌کارهای کارت (Case Workflows)

> **درس از sotcrm:** بعضی فرآیندها فقط معامله نیستن — بیعانه، تست درایو، فالوآپ.

### 3.1 گردش‌کار ساده
برای دمو، فقط ۲ گردش‌کار:

| گردش‌کار | مراحل |
|---|---|
| **فالوآپ مشتری** | تماس اول → یادآوری → تماس دوم → نتیجه |
| **پیگیری قرارداد** | قرارداد ثبت شده → پیگیری مالی → تایید نهایی → تحویل |

### 3.2 نمایش
- کارت‌های جداگانه در Sidebar (زیر «کارت‌ها»)
- هر گردش‌کار: Kanban ساده با مراحل مشخص
- ارتباط با معامله اصلی

---

## Phase 4 — گزارش‌ها + خروجی

> **درس از sotcrm:** مدیران به گزارش نیاز دارن، نه فقط لیست.

### 4.1 گزارش شخصی (من)
- فعالیت‌های امروز / ۷ روز / ۳۰ روز
- تماس‌ها بر اساس نتیجه
- معاملات جلو رفته
- نرخ اولین تماس زیر ۱ ساعت

### 4.2 خلاصه تیم
- هر فروشنده: چند تماس، چند معامله، چند برنده
- مقایسه عملکرد
- بهترین فروشنده

### 4.3 خروجی اکسل
- دانلود فایل اکسل از لیست فعلی
- فیلترهای اعمال شده روی خروجی

---

## Phase 5 — سیستم SMS + تماس

> **درس از sotcrm:** ارتباط با مشتری باید از داخل سیستم باشه.

### 5.1 پیامک
- اتصال به KavehNegar API
- قالب‌های آماده (خوش‌آمد، یادآوری، پیگیری)
- ارسال از صفحه جزئیات معامله
- تاریخچه پیامک‌ها در تایم‌لاین

### 5.2 تماس
- Click-to-call (لینک `tel:`)
- ثبت نتیجه تماس (S1-S5)
- شمارنده تلاش‌ها

---

## Phase 6 — هوش مصنوعی واقعی

> **درس از sotcrm:** AI باید قابل اعتماد باشه، نه فقط نمایشی.

### 6.1 Health Score واقعی
فرمول ساده:
```
health = 40 * (no_activity_penalty) 
       + 25 * (label_high ? 1 : 0) 
       + 15 * (stage_advanced ? 1 : 0) 
       + 10 * (time_in_stage / sla_ratio)
       + 10 * (source_top5 ? 1 : 0)
```

### 6.2 Next Best Action (规矩-based)
قواعد ساده:
- سرنخ جدید بدون تماس → «تماس بگیرید»
- معامله ۳ روز بدون فعالیت → «پیگیری کنید»
- معامله احتمال بالا → «ارجاع به مدیر»

### 6.3 پیشنهاد عنوان هوشمند
بر اساس: شرکت + مرحله + مخاطب → عنوان پیشنهادی

---

## Phase 7 — Multi-Tenant + Deploy

> **درس از sotcrm:** امنیت از اول، نه بعداً.

### 7.1 احراز هویت
- ورود با رمز عبور
- نقش‌ها: ادمین / مدیر / فروشنده
- حالت ساده/پیشرفته بر اساس نقش

### 7.2 داده چند-مستاجری
- هر شرکت داده جداگانه
- فیلتر خودکار بر اساس کاربر

### 7.3 Deploy
- Docker + Vercel/Netlify
- CI/CD با GitHub Actions
- Monitoring + Error tracking

---

## ترتیب اجرا

```
الان ← Phase 1 (امروز + ⌘K) ← Phase 2 (Capture)
     ← Phase 3 (Cases) ← Phase 4 (گزارش)
     ← Phase 5 (SMS) ← Phase 6 (AI)
     ← Phase 7 (Deploy)
```

**هر Phase = ۱-۲ روز کاری**
**هر Phase بعد از تکمیل: کامیت + پوش + آپدیت progress**

---

## قوانین طراحی (شخصی‌سازی شده)

### از sotcrm یاد گرفتیم:
1. **«امروز» صفحه اصلی باشه** — نه Dashboard
2. **ثبت سریع** — حداکثر ۳ تاپ برای هر عملیات
3. **Smart Gates** — مشورتی تا اجباری
4. **سلامت معامله** — عدد نه فقط رنگ
5. **تایمرها** — هر مرحله زمان مشخصی داره

### تفاوت با sotcrm:
1. **ساده‌تر** — ۶ مرحله نه ۷۱
2. **سریع‌تر** — Mock data نه Postgres
3. **انعطاف‌پذیرتر** — قابل تغییر آسان
4. **UI بهتر** — Focus روی زیبایی و سادگی

---

## موفقیت چطور سنجیده میشه؟

| معیار | هدف |
|---|---|
| زمان ثبت فعالیت | < ۳ ثانیه |
| زمان پیدا کردن معامله | < ۵ ثانیه (⌘K) |
| تعداد کلیک برای اقدام بعدی | ≤ ۲ |
| رضایت بصری | «漂亮» بگه |
| بدون آموزش | کاربر جدید بدون راهنما کار کنه |
